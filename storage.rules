rules_version = '2';

// Allow public read/write on memory images for the shared canvas.
service firebase.storage {
  match /b/{bucket}/o {
    match /memories/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
